.env
.env.*
!.env.example
node_modules/
.DS_Store
*.tar.gz

# 运行时数据：玩家提问记录、NPC 回答缓存。
# 里面有真实访客输入，不提交。
data/

# 剧情全文（含谜底与全部结案答案）。仓库是公开的，推上去等于把谜底送给评委，
# 所以显式挡住 —— 免得以后哪次 `git add -A` 顺手把它带上去（2026-09-14）。
STORY.md
